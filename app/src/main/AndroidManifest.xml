<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MySleepzyApp">
        <activity android:name=".SleepHistoryActivity" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".SecondActivity"
            android:exported="false"
            android:parentActivityName=".MainActivity" />

        <activity
            android:name=".ThirdActivity"
            android:exported="false"
            android:parentActivityName=".MainActivity" />

        <activity
            android:name=".ForthActivity"
            android:exported="false"
            android:parentActivityName=".MainActivity" />

        <activity android:name=".RegisterOrLoginPageActivity"
            android:exported="false"/>

        <activity android:name=".LoginActivity"
            android:exported="false"/>
        
        <!-- Added authentication screens -->
        <activity android:name=".RegisterActivity"
            android:exported="false"/>
        <activity android:name=".ForgotPasswordActivity"
            android:exported="false"/>

        <activity android:name=".MindfulTipsActivity" />

        <activity android:name=".MindfulTipsActivityTwo" />

        <activity android:name=".MindfulTipsActivityThree" />

        <activity android:name=".TrophiesActivity" />

        <activity android:name=".SleeptimeDashboardActivity" />
        <activity android:name=".SetBedtimeActivity" />
        <activity android:name=".SetWakeupActivity" />

        <!-- Profile screen -->
        <activity android:name=".ProfileActivity" />

        <!-- Account Information screen -->
        <activity android:name=".AccountInfoActivity" android:exported="false" />

        <!-- Single-activity host with fragments and bottom nav -->
        <activity android:name=".MainHostActivity" />

        <!-- Music player screen -->
        <activity android:name=".MusicPlayerActivity" android:exported="false" />





    </application>

</manifest>